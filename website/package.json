{
  "name": "claude-craft-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "bash scripts/sync-docs.sh && vitepress build",
    "preview": "vitepress preview",
    "sync": "bash scripts/sync-docs.sh",
    "test:e2e": "playwright test --config tests/e2e/playwright.config.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.61.0",
    "vitepress": "^1.6.3"
  },
  "overrides": {
    "vite": "^6.4.3",
    "esbuild": "^0.25.0"
  }
}
