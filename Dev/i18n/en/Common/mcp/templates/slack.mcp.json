{
  "_comment": "Required env vars: SLACK_BOT_TOKEN (xoxb-...), SLACK_TEAM_ID (T...). Set these in your shell environment before launching Claude Code.",
  "$schema": "https://claude.ai/mcp-schema.json",
  "mcpServers": {
    "slack": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-slack"],
      "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
        "SLACK_TEAM_ID": "${SLACK_TEAM_ID}"
      }
    }
  }
}
