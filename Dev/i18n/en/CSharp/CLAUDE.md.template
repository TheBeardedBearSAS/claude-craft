# {{PROJECT_NAME}} - C#/.NET

> Generated: {{GENERATION_DATE}} | Claude Craft v{{VERSION}}

## What (Stack & Structure)

**Stack**: .NET {{DOTNET_VERSION}}, C# 13, Clean Architecture, CQRS, MediatR

### Project Structure
```
src/
├── Domain/           # Core business logic (no dependencies)
│   ├── Entities/     # Aggregate roots
│   ├── ValueObjects/ # Immutable types
│   └── Interfaces/   # Repository contracts
├── Application/      # Use cases, CQRS
│   ├── Commands/     # Write operations
│   └── Queries/      # Read operations
├── Infrastructure/   # External concerns
│   └── Data/         # EF Core, repositories
└── WebAPI/           # Presentation layer
```

## Why (Purpose & Organization)

### Architecture: Clean Architecture + DDD + CQRS
- **Domain** layer has ZERO external dependencies
- **Application** orchestrates use cases via MediatR
- **Infrastructure** implements technical details

See `rules/02-architecture-csharp.md` for complete patterns.

### Critical Rules
| Rule | Reason |
|------|--------|
| Nullable reference types | Null safety |
| CancellationToken always | Async best practices |
| Repository pattern | Data access abstraction |
| 80%+ coverage | Quality assurance |

## How (Commands & Workflows)

### Development Commands
```bash
dotnet build          # Build solution
dotnet run            # Start API
dotnet test           # Run tests
dotnet format         # Format code
```

### Quality Pipeline
```bash
dotnet format --verify-no-changes  # Check formatting
dotnet build -warnaserror          # Build with warnings as errors
dotnet test --collect:"XPlat Code Coverage"
```

### Available Skills
- `/csharp:check-compliance` - Full compliance audit
- `/csharp:check-architecture` - Architecture validation
- `/csharp:generate-feature` - Generate CQRS feature

## References

| Topic | Rule File |
|-------|-----------|
| Architecture | `rules/02-architecture-csharp.md` |
| Coding Standards | `rules/03-coding-standards.md` |
| SOLID | `rules/04-solid-principles.md` |
| Testing | `rules/07-testing-csharp.md` |
| Quality | `rules/08-quality-tools.md` |
| Security | `rules/11-security-csharp.md` |

<!-- PROJECT-SPECIFIC-START -->
## Project Specifics

{{TECH_STACK}}

<!-- PROJECT-SPECIFIC-END -->
