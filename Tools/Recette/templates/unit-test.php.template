<?php

declare(strict_types=1);

namespace Tests\Unit\Regression;

use PHPUnit\Framework\TestCase;
use PHPUnit\Framework\Attributes\Group;

/**
 * Regression test for error: {{ERROR_ID}}
 *
 * Description: {{DESCRIPTION}}
 *
 * Golden Rule: This test ensures the bug never reappears.
 * A fixed bug should NEVER be reproduced.
 *
 * @generated by Recette
 * @see .recette/regression/registry.yaml
 */
#[Group('regression')]
class {{CLASS_NAME}}Test extends TestCase
{
    /**
     * Test that the error condition from {{ERROR_ID}} does not occur.
     *
     * @regression {{ERROR_ID}}
     */
    public function test_{{METHOD_NAME}}_does_not_reproduce_{{ERROR_ID}}(): void
    {
        // Arrange
        // Set up the conditions that caused the error
        {{ARRANGE}}

        // Act
        // Perform the action that triggered the error
        {{ACT}}

        // Assert
        // Verify the error condition does not occur
        {{ASSERT}}
    }

    /**
     * Test edge case related to {{ERROR_ID}}.
     *
     * @regression {{ERROR_ID}}
     */
    public function test_{{METHOD_NAME}}_edge_case_{{ERROR_ID}}(): void
    {
        // Arrange
        {{ARRANGE_EDGE_CASE}}

        // Act
        {{ACT_EDGE_CASE}}

        // Assert
        {{ASSERT_EDGE_CASE}}
    }

    /**
     * Create the subject under test.
     */
    private function createSubject(): mixed
    {
        // TODO: Create the subject under test
        // Based on context: {{CONTEXT}}
        return null;
    }
}
